import Modal from "./Modal"

export default function EditTaskModal({ show, onClose, task, onSave }) {




    return (

        <Modal
            title="Modifica Task"
            content={ }
            confirmText="Salva"
            onConfirm={ }
        />
    )


};
